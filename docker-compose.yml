version: '3'
services:
  web:
      container_name: process_receipts
      build: .
      command: bundle exec rails s -p 3000 -b '0.0.0.0'
      # volumes:
      #   - ./app:/app
      ports:
        - "3000:3000"
